# 更新日志

本文件记录用户画像插件的版本更新，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.10.1] - 2026-09-24

### 新增
- 新增 `silent_for_banned`（默认开启）：调用者在 qq_tools 黑名单中时，所有画像命令（`/画像`、`/我`、`/我的画像`、`/画像删除`、`/画像清理`、`/画像扫描` 及斜杠兜底）一律静默不回复；发言数据采集不受影响，照常静默积累

### 修复
- 修复黑名单用户仍可正常使用画像命令的问题：此前黑名单只用于 `ban_history` 前科展示，命令入口没有任何黑名单检查

## [1.10.0] - 2026-09-21

### 新增
- 可信插件写入 API 返回明确状态，单一 KV 账户原子保存近期明细、累计风险和独立 seen 账本；明细截断不冲掉风险/去重键，失败可重试，删除和淘汰同步清理
- 即时生成个人警告标签、可读前科及最多 100 分的综合风险；无聊天记录亦可查询，文字和图片同步展示
- `/画像删除` 和容量淘汰通过守卫可信 API 取消 pending 画像投递，持久删除界限与双阶段清理阻止在途旧事件复活；删除界限读取失败时禁言事件写入 fail closed 且可重试加载，direct blacklist 不受影响

## [1.9.6] - 2026-09-19

### 新增
- 新增 `llm_refresh_interval`（默认 3600 秒）：即使发言材料变化，距上次成功分析不足该间隔仍沿用旧结论，到期后下一次查询自动重算；设为 0 恢复"材料一变立即重算"
- LLM 画像改为增量修正：重算时把上一次的分析结论（历史标签、历史印象、历史特征）作为先验传入 prompt，要求新材料不能明确反驳就保留历史标签，印象在历史印象基础上做有限更新，不再整段覆盖

### 修复
- 修复短时间大量发言即可冲刷掉既有 LLM 印象的风险：此前每条新消息都会改变材料指纹并立即重算，且新结果完全覆盖旧结果，存在"刷量洗白"绕过邀请守卫判断的攻击面

## [1.9.5] - 2026-09-14

### 修复
- 适配 AstrBot 4.28 默认模型新路径 `agent_runner.config.model.provider_id`；不再因旧 `provider_settings.default_provider_id` 为空而静默生成空画像
- 历史扫描材料发生变化时立即作废旧 LLM 画像缓存，下一次 `/我` 或 `/画像` 自动重新生成
- 空 LLM 结果改用失败短缓存时间，不再按成功结果缓存 24 小时；找不到 provider 时写入明确错误日志
- 补齐持久化历史计数与作用域字段的加载规范化，LLM 材料指纹纳入历史消息数

## [1.9.4] - 2026-09-14

### 修复
- 适配新版 AstrBot“持久化群聊消息记录”：从 `platform_message_history` 按 `sender_id` 精确读取当前平台各已知群的历史消息，不再只依赖旧 `conversations` 会话历史
- 旧画像历史扫描版本会在首次查询时自动升级重扫，恢复老群友首次/最近发言时间和最近原话材料
- 历史消息量与插件自有计数取较大值而非相加，避免同一消息被重复统计；纯媒体占位消息计入活跃度但不作为 LLM 原话

### 优化
- 同一群持久化历史使用 30 秒只读缓存并合并并发请求，批量画像扫描不会为每个 QQ 重复读取同一批消息
- 新增 `platform_history_scan_limit`，默认每群读取最近 700 条，与 AstrBot 默认持久化上限一致

## [1.9.3] - 2026-09-12

### 优化
- 关键统计格改为四色柔和底色卡片，标签与数值居中对齐并按模块着色，替代原白底描边贴边样式

## [1.9.2] - 2026-09-12

### 新增
- 图片卡片每个展示模块独立开关：`card_show_tags` / `card_show_stats` / `card_show_impression` / `card_show_traits` / `card_show_social` / `card_show_criminal`，全部关闭时只保留头部与页脚

### 优化
- 重排图片卡片视觉：白环圆形头像、右侧按风险分着色的风险徽章、头部风险色描边条、带强调条的模块标题、放大的统计数值、人物印象高亮卡片与居中页脚生成时间

## [1.9.1] - 2026-09-11

### 修复
- 接入邀请守卫新版证据接口时补回按群禁言次数，「该群累计禁言 bot N 次」标注恢复显示

## [1.9.0] - 2026-09-09

### 新增
- 重做用户画像卡片与结构化分析
- 决策画像新增综合印象、性格特质、数据新鲜度与 LLM 分析状态，供加群邀请守卫消费

## [1.8.0] - 2026-09-09

### 新增
- 强化画像采集分析与隐私控制：群聊查询不泄露私聊摘录与好友验证语等敏感内容

## [1.7.2] - 2026-09-06

### 文档
- 明确 LLM 驱动的画像分析流程说明

## [1.7.1] - 2026-09-06

### 文档
- 完善插件市场发布信息与说明

## [1.7.0] - 2026-09-04

### 新增
- 增强邀请记录兼容性，提供结构化决策画像接口供邀请守卫调用

## [1.6.0] - 2026-09-03

### 新增
- 好友/进群社交来源采集与展示（添加好友时间、验证语、进群方式）

## [1.5.2] - 2026-09-03

### 修复
- 批量历史扫描只处理未完成对象，支持续扫不漏扫

## [1.5.1] - 2026-09-03

### 修复
- `/画像扫描` 增加正则兜底，不依赖 wake_prefix

## [1.5.0] - 2026-09-03

### 新增
- 历史扫描回填，新人判定支持历史感知

### 修复
- 修复空集合导致的崩溃

## [1.4.0] - 2026-09-03

### 变更
- 重构画像查询权限与配置体系，统一命令及 LLM 工具校验

## [1.3.2] - 2026-09-03

### 修复
- 正确读取分组配置中的 show_quotes

## [1.3.1] - 2026-09-02

### 新增
- show_quotes 开关控制是否显示发言摘录

### 修复
- 自查询命令（`/我的画像`、`/查自己`、`/我`）响应与权限顺序问题
- 图片输出兼容性与发送方式修复

## [1.3.0] - 2026-09-02

### 新增
- 查询权限控制（仅查自己/指定群全员可查）
- `/我的画像` 命令
- LLM 标签 TTL 缓存，图片渲染优化

## [1.2.0] - 2026-09-01

### 新增
- 综合风险分（支持自定义权重与风险等级）
- 行为信号采集：图片/链接/二维码/@/消息长度/夜间活跃
- 图片输出模式，Pillow 渲染画像卡片避免刷屏

### 修复
- 修复配置 schema 中文引号导致的 JSON 无效
- 图片输出发送与 Pillow 旧版兼容性问题

## [1.1.0] - 2026-08-31

### 变更
- 重构为用户画像/自动标签引擎，输出结构化标签供加群邀请守卫决策调用
- 完善配置体系

## [1.0.0] - 2026-08-31

### 新增
- 首个版本：持续积累群聊/私聊发言，LLM 生成用户画像与标签
